package a5;

// Create a shrimp subclass that extends the ParentIngredient superclass
public class Shrimp extends ParentIngredient {
	
	public Shrimp() {
		super("shrimp", 0.65, 32, false, false, true);
		}

	}